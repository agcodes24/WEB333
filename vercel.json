{
  "version": 2,
  "builds": [{ "src": "api/index.js", "use": "@vercel/node" }],
  "routes": [
    { "handle": "filesystem" },               // <-- serve /public files first
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}
